#include <stdio.h>
/**
 * main- Serves as the entry point for program
 *
 * Return:By default returns zero if no error otherwise returns non-zero value
 */
int main(void)
{
	puts("\"Programming like building a nultilingual puzzle");
	return (0)
}
